language: java
jdk:
  - openjdk8
branches:
  only:
    - master
# Travis CI 서버의 Home
cache:
  directories:
    - '$HOME/.m2/repository'
    - '$HOME/.gradle'
script: "./gradlew clean build"

# CI 실행 완료시 알람12
notifications:
  email:
    recipients:
      - hj.kim.aracomm@gmail.com
      on_success: always
      on_failure: always
